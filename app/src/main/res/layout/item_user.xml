<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="8dp"
    android:layout_marginBottom="8dp"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/friendProfileImage"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/icon_profile"
        android:scaleType="centerCrop"
        android:padding="3dp"
        android:clipToOutline="true" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/friendNameText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Full Name"
            android:textColor="#D6C84E"
            android:textSize="15sp"
            android:fontFamily="sans-serif-medium"
            android:shadowColor="#BF9D2F"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="2" />

        <TextView
            android:id="@+id/friendUsernameText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@username"
            android:textColor="#9CA3AF"
            android:textSize="12sp"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Friend Badge (shown when already friends) -->
    <TextView
        android:id="@+id/friendBadge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Friends"
        android:textColor="#1F1F0F"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/icon_box"
        android:paddingHorizontal="12dp"
        android:paddingVertical="4dp"
        android:visibility="gone" />

    <!-- Add Friend Button (shown when not friends) -->
    <ImageView
        android:id="@+id/friendActionButton"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/icon_connect"
        app:tint="#D6C84E"
        android:contentDescription="Add Friend" />
</LinearLayout>